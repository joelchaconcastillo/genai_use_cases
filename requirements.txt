

## Core LangChain packages
langchain>=0.1.0
langchain-community>=0.1.0
langchain_huggingface>=0.0.1
langchain_chroma>=0.0.1
langsmith>=0.1.0

## Tokenizers and helpers
tiktoken
sentence-transformers
beautifulsoup4
lxml

## Transformers / model runtime (optional heavy installs)
transformers>=4.30.0
torch>=2.0.0
sentencepiece
faiss-cpu
accelerate

## Vector store / database
chromadb>=0.3.0

## Front-end app (Streamlit)
streamlit

## Backend API (FastAPI + Uvicorn)
fastapi[all]

## Optional extras for Chroma / HuggingFace
requests
pydantic>=2.0

## Env and config
pyyaml
python-dotenv

## Note: `google-generative-ai` is not published under that exact name on PyPI
## (remove it); `langchain-google-genai` is included above as the LangChain
## provider integration. If you prefer using Google's official client directly,
## install `google-generative-ai` by the correct package name when available.

